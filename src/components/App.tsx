import { observer } from 'mobx-react'
import * as React from 'react'

import './App.css'

const App: React.FC = () => {
  return (
    <div className="app">
      Test
    </div>
  )
}

export default observer(App)
